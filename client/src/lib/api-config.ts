export const API_BASE = ""; // Relative path to leverage Vite proxy
export const API_URLS = {
    health: `/api/health`,
    fetchInfo: `/api/fetch-info`,
    streamDownload: `/api/downloads/stream`,
    downloads: `/api/downloads`,
    settings: `/api/settings`,
};
